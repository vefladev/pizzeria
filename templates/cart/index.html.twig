{% extends 'base.html.twig' %}

{% block title %}Mon panier{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center my-4" id="order">Récapitulatif de votre panier</h2>
        <div class="my-2">
            {% for product in products %}

                <div class="card mb-3">
                    <div class="row g-0">
                        <div class="col-md-4">
                            <img src="{{ product.image }}" class="img-fluid rounded-start"
                                 alt="image de {{ product.name }}">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body">
                                <h5 class="card-title">{{ product.name }}</h5>
                                <div class="card-text card-text-home">
                                    <p>
                                        {{ product.description | raw }}
                                    </p>
                                </div>
                                <a class="btn btn-danger" href="#">Enlever <i class="fas fa-trash-alt"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="text-center">
                    <img src="{{ asset("img/empty-cart-img.png") }}" class="empty-cart-img"/>
                    <p>Votre panier est vide.</p>
                    <a href="/#order" class="btn btn-warning">Passer commande</a>
                </div>
            {% endfor %}

        </div>
    </div>
{% endblock %}
